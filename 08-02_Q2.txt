Q. write a docker file set bash image to ubuntu workdir /var/www/html
base image ubuntu 

dir  to /var/www/html
install nginx

copy index.html to workdir

Q. create user image which is using ubuntu  as base layer  then install apache2 and 
expose it 80/udp alos specify enterypoint for the image